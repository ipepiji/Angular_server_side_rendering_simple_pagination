export enum MainRouter {
    default = 'auth',
    notFound = '404'
}

export enum AuthRouter {
    default = 'login',
    notFound = '404'
}

export enum PagesRouter {
    default = 'homepage',
    notFound = '404'
}